@using VisReg.Cmn.Namings

@{ ViewBag.Title = NamingStrings.TitleAppShortName; }

<div class="container">
    <div class="panel panel-primary">

        <div class="panel-heading">
            <div class="panel-title text-center"><strong>ΛΙΣΤΑ ΧΡΗΣΤΩΝ ΕΦΑΡΜΟΓΗΣ</strong></div>
        </div>

        <div class="panel-body">
            <div class="row" style="padding-top:6px; padding-bottom:6px">
                @*<div class="col-md-2">
                    <button type="submit" class="btn btn-primary btn-lg" id="btnGetUser">
                        <span class="glyphicon glyphicon-search"></span><strong> ΕΚΤΕΛΕΣΗ ΑΝΑΖΗΤΗΣΗΣ </strong>
                    </button>
                </div>*@

                @* ΔΗΜΙΟΥΡΓΙΑ ΧΡΗΣΤΗ  *@
                <div class="col-md-2">
                    <button type="button" class="btn btn-success btn-group-lg" id="btnCreateUser"
                            onclick="location.href='@Url.Action("CreateUser", "User")'">
                        <span class="glyphicon glyphicon-user"></span> <strong> @NamingStrings.BtnCreateUser </strong>
                    </button>
                </div>
            </div>

            <div class="clearfix"></div>

            @* ΛΙΣΤΑ ΧΡΗΣΤΩΝ (Grid) *@
            <div id="divEmpGrid" style="margin-top:6px;">
                @{ Html.RenderPartial("GetUsersGrid"); }
            </div>
        </div>

    </div>
</div>


@*<script type="text/javascript">
    $(document).ready(function () {
        $("#btnGetUser").click(function () {
            $("#grdUsers").data("kendoGrid").dataSource.read();
        });

        // return
        $(document).bind('keydown', function (e) {
            if (e.which == 13) {
                $('#btnGetUser').trigger('click');
            }
        });
    });
</script>*@ 